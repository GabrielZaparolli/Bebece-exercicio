declare const useCumulativeHeightState: () => {
    getAccumulatedHeight: (index: number) => number;
    updateRowHeight: ({ height, index, }: {
        height: number;
        index: number;
    }) => void;
};
export default useCumulativeHeightState;
