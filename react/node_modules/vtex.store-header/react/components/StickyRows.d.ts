import type { PropsWithChildren } from 'react';
import React from 'react';
declare type RowContextType = {
    /** Callback executed when element is resized */
    onResize(height: number): void;
    /** Vertical Offset from top of container */
    offset: number;
};
declare const RowContext: React.Context<RowContextType>;
/**
 * @deprecated Use the `vtex.sticky-layout` app instead
 */
declare function StickyRows({ children }: PropsWithChildren<{}>): JSX.Element;
export default StickyRows;
export { RowContext };
