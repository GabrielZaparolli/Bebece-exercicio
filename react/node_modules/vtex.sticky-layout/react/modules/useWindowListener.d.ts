/**
 * Hook responsible for handling multiple event callbacks
 * by using a single listener per event
 * @export
 * @param {string[]} eventList
 * @param {Function} fn
 */
export declare function useWindowListener(eventList: string[], fn: Function): void;
