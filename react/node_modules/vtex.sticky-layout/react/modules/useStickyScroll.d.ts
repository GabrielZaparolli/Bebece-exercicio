import { Positions } from '../types';
interface StickyProps {
    position?: Positions;
    verticalSpacing?: number;
    stickOffset?: number;
    contentHeight: number | string;
    wrapperOffsetTop: number | undefined;
}
export declare const useStickyScroll: ({ position, verticalSpacing, stickOffset, contentHeight, wrapperOffsetTop, }: StickyProps) => {
    isStuck: boolean;
};
export {};
