import { FC } from 'react';
import { Positions } from './types';
interface Props {
    position?: Positions;
    verticalSpacing?: number;
    zIndex?: number;
}
declare const StickyLayoutComponent: FC<Props>;
export default StickyLayoutComponent;
