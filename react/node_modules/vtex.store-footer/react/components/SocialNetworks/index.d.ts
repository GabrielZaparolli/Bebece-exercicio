import type { PropsWithChildren } from 'react';
import type { CssHandlesTypes } from 'vtex.css-handles';
export declare const CSS_HANDLES: readonly ["socialNetworksTitle", "socialNetworksContainer", "socialNetworkWrapper", "socialNetworkLink", "socialNetworkImage"];
interface SocialNetworkData {
    url: string;
    name: string;
}
interface Props {
    /** Section title */
    title?: string;
    /** List of social networks to display */
    socialNetworks: SocialNetworkData[];
    /** Define if social network icons will be coloured */
    showInColor: boolean;
    classes?: CssHandlesTypes.CustomClasses<typeof CSS_HANDLES>;
}
declare function SocialNetworks({ title, showInColor, socialNetworks, classes, }: PropsWithChildren<Props>): JSX.Element;
declare namespace SocialNetworks {
    var schema: {
        title: string;
        description: string;
        type: string;
        properties: {
            showInColor: {
                default: boolean;
                isLayout: boolean;
                title: string;
                type: string;
            };
        };
    };
}
export default SocialNetworks;
