/// <reference types="react" />
export declare const CSS_HANDLES: readonly ["paymentMethodIcon", "paymentMethodIconImage"];
export declare type PaymentMethod = 'diners club' | 'mastercard' | 'visa';
interface Props {
    imageSrc?: string;
    /** If true, the original logo (with color) is used. If not, the grayscale's one */
    showInColor?: boolean;
    /** Indicates which one of the payments method should the component show its image */
    paymentMethod: PaymentMethod;
}
/**
 * Shows an image for the payments forms accepted
 */
declare function PaymentMethodIcon({ paymentMethod, showInColor }: Props): JSX.Element | null;
export default PaymentMethodIcon;
