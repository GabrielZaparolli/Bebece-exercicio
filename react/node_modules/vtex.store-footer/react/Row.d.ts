import type { PropsWithChildren } from 'react';
interface Props {
    /** Define if the row should take the full width of its container */
    fullWidth?: boolean;
    /** Define if background and text colors should be inverted */
    inverted?: boolean;
    blockClass?: string;
}
declare function Row({ children, fullWidth, inverted }: PropsWithChildren<Props>): JSX.Element;
declare namespace Row {
    var schema: {
        title: string;
        type: string;
        properties: {
            blockClass: {
                title: string;
                description: string;
                type: string;
                isLayout: boolean;
            };
            inverted: {
                title: string;
                description: string;
                type: string;
                default: boolean;
                isLayout: boolean;
            };
            fullWidth: {
                title: string;
                description: string;
                type: string;
                default: boolean;
                isLayout: boolean;
            };
        };
    };
}
export default Row;
