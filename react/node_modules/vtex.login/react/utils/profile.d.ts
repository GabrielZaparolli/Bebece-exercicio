import type { Session } from '../types/RenderSession';
export interface Profile {
    email: string | null;
    firstName: string | null;
    isAuthenticated: boolean;
}
export declare const getProfile: (data?: Session | undefined) => Profile | null;
