/**
 * Set Cookie from the URL name=value and clean up the URL query params.
 * Used in the OAuth login.
 *
 * @param {String} url To set cookie and cleaned up.
 */
export declare const setCookie: (url: any) => void;
