import type { Dispatch, FC, RefObject } from 'react';
import type { Steps } from '../utils/steps';
interface Ctx {
    step: Steps | null;
    setStep: Dispatch<Steps | null>;
    alwaysShowLoginOptions: boolean;
    inputToFocusRef: RefObject<HTMLInputElement>;
}
export declare const useStateMachine: () => Ctx;
export declare const StateMachineProvider: FC;
export {};
