import type { FC } from 'react';
import type { Ctx as BlockProps } from '../../contexts/loginBlockProps';
interface LoginProps extends BlockProps {
    isHeaderLogin?: boolean;
}
declare const LoginProvider: FC<LoginProps>;
export default LoginProvider;
