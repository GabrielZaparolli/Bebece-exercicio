import type { FC } from 'react';
import type { IdProviders } from 'vtex.react-vtexid';
interface Props {
    fallbackTitle: string;
    options: IdProviders;
    className?: string;
}
declare const Options: FC<Props>;
export default Options;
