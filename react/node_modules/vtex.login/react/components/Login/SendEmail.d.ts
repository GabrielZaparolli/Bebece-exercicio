import type { FC } from 'react';
import type { Steps } from '../../utils/steps';
interface Props {
    prevStep: Steps;
    onSendMail: () => void;
    onGetPhoneNumber: () => void;
    showBackButton?: boolean;
}
declare const SendEmail: FC<Props>;
export default SendEmail;
