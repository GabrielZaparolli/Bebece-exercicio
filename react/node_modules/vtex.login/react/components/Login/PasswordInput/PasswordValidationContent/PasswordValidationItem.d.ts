import type { FC, ReactNode } from 'react';
interface Props {
    valid?: boolean;
    prefix: string;
    label: ReactNode;
}
declare const PasswordValidationItem: FC<Props>;
export default PasswordValidationItem;
