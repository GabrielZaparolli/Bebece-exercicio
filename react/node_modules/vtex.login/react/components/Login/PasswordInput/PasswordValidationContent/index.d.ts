import type { FC, ReactNode } from 'react';
export interface Field {
    id: number;
    prefix: string;
    label: ReactNode;
    valid?: boolean;
}
interface Props {
    fields: Field[];
}
declare const PasswordValidationContent: FC<Props>;
export default PasswordValidationContent;
