import { FC, ReactElement, ComponentType } from 'react';
import { SearchQuery } from './modules/searchTypes';
interface SearchRouteParams {
    term?: string;
    department?: string;
    category?: string;
}
interface GetHelmetLinkParams {
    canonicalLink: string | null;
    page: number;
    map?: string;
    term?: string;
    rel: 'canonical' | 'prev' | 'next';
}
export declare function getHelmetLink({ canonicalLink, page, map, rel, term, }: GetHelmetLinkParams): {
    rel: "canonical" | "prev" | "next";
    href: string;
} | null;
interface SearchWrapperProps {
    children: ReactElement;
    params: SearchRouteParams;
    searchQuery: SearchQuery;
    orderBy?: string;
    to?: number;
    page?: number;
    maxItemsPerPage?: number;
    CustomContext?: ComponentType;
    facetsLoading?: boolean;
}
declare const SearchWrapper: FC<SearchWrapperProps>;
export default SearchWrapper;
