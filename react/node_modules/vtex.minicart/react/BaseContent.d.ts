import { FC } from 'react';
import { CssHandlesTypes } from 'vtex.css-handles';
declare const CSS_HANDLES: readonly ["minicartContentContainer", "minicartProductListContainer", "minicartTitle", "minicartFooter"];
interface Props {
    sideBarMode: boolean;
    finishShoppingButtonLink: string;
    classes?: CssHandlesTypes.CustomClasses<typeof CSS_HANDLES>;
}
declare const Content: FC<Props>;
export default Content;
