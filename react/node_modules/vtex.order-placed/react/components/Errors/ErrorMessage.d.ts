import { ReactNode, FC } from 'react';
interface Props {
    errorId: string;
    messageId?: string;
    children?: ReactNode;
    icon: ReactNode;
}
declare const ErrorMessage: FC<Props>;
export default ErrorMessage;
