import { FC, ReactNode } from 'react';
interface Props {
    content: ReactNode[];
    beforeTitleLabel?: ReactNode;
    titleLabel: ReactNode;
    toggleLabel?: ReactNode;
}
declare const AttachmentAccordion: FC<Props>;
export default AttachmentAccordion;
