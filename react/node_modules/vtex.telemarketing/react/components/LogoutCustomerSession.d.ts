import { ReactNode } from 'react';
interface Props {
    /** Signed in client */
    client: Client;
    /** Loading Status */
    loading: boolean;
    /** Calls the depersonify on the parent component */
    onDepersonify: () => any;
    /** Current signedin attendant email */
    attendantEmail: string;
    /** If is mobile or not */
    mobile: boolean;
    /** Children */
    readonly children?: ReactNode;
}
/** Component that shows the client info calls the setSession function  to logout. */
declare const LogoutCustomerSession: (props: Props) => JSX.Element;
export default LogoutCustomerSession;
