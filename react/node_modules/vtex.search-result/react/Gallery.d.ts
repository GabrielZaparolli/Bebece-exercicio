import React from 'react';
import type { GalleryLayoutProps, Slots } from './GalleryLayout';
import type { GalleryProps as GalleryLegacyProps } from './GalleryLegacy';
declare type GalleryLayoutPropsWithSlots = Omit<GalleryLayoutProps, 'slots'> & Slots;
declare const Gallery: React.FC<GalleryLegacyProps | GalleryLayoutPropsWithSlots>;
export interface Product {
    /** Product's id. */
    productId: string;
    /** Product's cache id. */
    cacheId: string;
    /** Product's name. */
    productName: string;
    /** Product's description. */
    description: string;
    /** Product's categories. */
    categories: unknown[];
    /** Product's link. */
    link?: string;
    /** Product's link text. */
    linkText: string;
    /** Product's brand. */
    brand?: string;
    /** Product's SKU items. */
    items: ProductItem[];
}
interface ProductItemReference {
    Value: string;
}
interface ProductItemImage {
    /** Images's imageUrl. */
    imageUrl: string;
    /** Images's imageTag. */
    imageTag: string;
}
interface ProductItemSeller {
    /** Sellers's commertialOffer. */
    commertialOffer: {
        /** CommertialOffer's price. */
        Price: number;
        /** CommertialOffer's list price. */
        ListPrice: number;
    };
}
interface ProductItem {
    /** SKU's id. */
    itemId: string;
    /** SKU's name. */
    name: string;
    /** SKU's referenceId. */
    referenceId?: ProductItemReference[];
    /** SKU's images. */
    images: ProductItemImage[];
    /** SKU's sellers. */
    sellers: ProductItemSeller[];
}
export default Gallery;
