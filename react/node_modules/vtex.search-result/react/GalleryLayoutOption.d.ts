import type { PropsWithChildren } from 'react';
interface GalleryLayoutOptionProps {
    /** Unique name to match with a layout name */
    name: string;
}
declare function GalleryLayoutOption({ name, children, }: PropsWithChildren<GalleryLayoutOptionProps>): JSX.Element;
export default GalleryLayoutOption;
