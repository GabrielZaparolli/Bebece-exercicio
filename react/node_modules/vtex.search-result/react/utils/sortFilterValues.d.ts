import { ASC, DESC } from '../constants';
interface Filters {
    key: string;
    quantity: number;
    title: string;
    type: string;
    facets: Facets[];
}
interface Facets {
    name: string;
    quantity: number;
}
interface SortRules {
    key: string;
    orderBy: keyof Facets;
    order: typeof ASC | typeof DESC;
}
export declare const sortFilterValues: (filters: Filters[], sortingRules: SortRules[]) => Filters[];
export {};
